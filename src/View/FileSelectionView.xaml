<UserControl x:Class="Effektive_Praesentationen.View.FileSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extension="clr-namespace:Effektive_Praesentationen.Extension"
             xmlns:local="clr-namespace:Effektive_Praesentationen.View" xmlns:viewmodel="clr-namespace:Effektive_Praesentationen.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:FileSelectionViewModel}"
             mc:Ignorable="d"
             Background="#ffffff"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Name="ImagePanel" VerticalAlignment="Center" HorizontalAlignment="Center" >
            <Button Background="Transparent" extension:DropFilesBehaviorExtension.IsEnabled="True"  Height="200" Width="400">Drop Here</Button>
            <TextBlock x:Name="defaultChapterPathText" Text="{Binding Chapters.DefaultChapter}"/>
        </StackPanel>
        <Button Content="Present" Height="70" Width="120" VerticalAlignment="Bottom" HorizontalAlignment="Right"
        Margin="50" Command="{Binding NavigateToInactiveLoopCommand}"></Button>
    </Grid>
</UserControl>
